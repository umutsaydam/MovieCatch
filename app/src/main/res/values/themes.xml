<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MovieCatch" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MovieCatch" parent="Base.Theme.MovieCatch" />

    <style name="ThemeBg">
        <item name="android:background">@color/white</item>
    </style>

    <style name="ThemeCardView">
        <item name="cardBackgroundColor">@color/white</item>
    </style>

    <style name="ThemeTitleTextColor">
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="ThemeSubTitleTextColor">
        <item name="android:textColor">@color/end_color</item>
    </style>

    <style name="IconTheme">
        <item name="tint">@color/black</item>
    </style>

    <style name="TrailerTint">
        <item name="backgroundTint">@color/white</item>
    </style>

    <style name="ToolbarTheme">
        <item name="contentScrim">@color/toolbar_color</item>
    </style>
</resources>